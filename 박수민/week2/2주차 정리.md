# Spring JPA

# JPA & Spring Data JPA 기초 - 최범균

**[섹션 1]**

persistence.xml 파일 영속성에 대한 정보를 담음

JPA

자바 객체와 관계형 DB 간의 매핑 처리를 위한 API

Hibernate DB 마다 알맞은 쿼리 생성: dialect

jpa에서는 entity 단위로 DB 테이블이랑 클래스 매핑

EntityManagerFactory : DB 연동을 위한 기반

EntityManager : 실제 DB 연동 처리

user 객체를 만들고 persist에 저장

find로 조회 후 transaction 후 commit

[01 정리]

1. 간단한 설정으로 클래스와 테이블 간 매핑 처리
    1. @Entity / @Table / …
2. EntityManager를 이용해서 DB 연동 처리
    1. persist / find …
3. 객체 변경만으로 DB 테이블 업데이트
4. 쿼리 작성 X (→ JPA 이용의 가장 큰 이유)

[02 - 코드 구조, 영속 컨텍스트]

EntityManagerFactory는 영속 단위 기준으로 생성됨

Application을 처음 한 번 구동할 때 한 번만 생성

close()로 팩토리 닫음

Factory를 사용해서 EntityManager 생성

insert Query는 언제 실행? → commit이 되는 시점 (식별자를 직접 설정하는 경우)

영속 컨텍스트: DB에서 읽어온 객체, EntityManager를 통해 저장한 객체 등을 저장하고 있는 메모리 공간

→ 커밋하는 시점에 영속 컨텍스트의 변경 내역을 DB에 반영(변경 쿼리 실행)

[03 - 엔티티 단위 CRUD 처리]

find 메소드는 존재하지 않으면 null값 반환

remove 메소드에 전달하는 객체는 find 메소드로 읽어온 객체를 전달해야함

[04 - 엔티티 맵핑]

접근 타입

- 필드 접근
- 프로퍼티 접근

- 설정 방법

@ID 어노테이션을 필드에 붙여서 필드 접근

ordinal 사용 적음

[05 - 엔티티 식별자 생성 방식]

4가지

1. 직접 할당
2. 식별 칼럼 방식
3. 시퀀스 사용 방식
4. 테이블 사용 방식

[06 - Embeddable]

@Embeddable을 사용해 개별 속성을 모아서 이해

[07 - 엔티티를 여러 테이블에 맵핑]

방법 1) @secondaryTable + 테이블명

방법 2) @secondaryTable + @AttributeOverride